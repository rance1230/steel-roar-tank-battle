# BASELINE — v2 功能基线（PHASE 0）

> 任何 v3 重构不得导致以下功能回退。回归套件：`/tmp/tanktest/verify6.js`（主流程 39 项）、`verify7.js`（触屏 12 项）、`hdshoot.js`（17 画面截图矩阵）。

## 状态机
`title → intro → play → clear → upgrade → (next|win) → NG+`；`play → over → (R 重试 | Q 标题)`；任意 play 中 `P/Esc/Start → pause` 菜单。

## 操作
- 键盘：WASD/方向键移动；J 机枪(按住)；K 主炮；L 蓄力导弹(按住松发)；U 空袭；Shift 涡轮；空格 护盾(瞬间格挡)；P 暂停；M 全局静音；Enter 确认；Esc 返回。
- 手柄（Gamepad API，蓝牙/有线统一）：左摇杆/十字键移动；动作键可自定义映射（OPTION→手柄映射，9 动作，捕获式分配，一键恢复默认，localStorage 持久化）；手柄可导航全部菜单；A 确认 / B 返回 / Start 暂停。
- 触屏：11 个半透明虚拟按钮（背景 alpha 0.10，均带文字），D-pad+机枪/主炮/导弹/空袭/加速/护盾/暂停；设置项 自动/开/关；触屏模式隐藏键盘提示、分数与暂停键错位避让；菜单/整备画面可直接点按（画布命中测试）。

## 设置（OPTION，标题与暂停菜单均可进）
语言 中/日/英（实时切换）；难度 5 档；BGM 音量 5 档；SE 音量 5 档（独立总线即时生效）；虚拟按钮开关；手柄映射；恢复默认。全部持久化 `localStorage.trSet`。

## 难度（5 档）
影响敌军 AI（预判/走位/残血后撤）、反应速度、耐久、精准度、掉落率、报酬倍率（1.0×~2.0×）。

## 战斗
- 武器：机枪 12/s、主炮 AoE、蓄力追踪导弹、空袭(5s CD，7 弹)、涡轮(限时+过热)、护盾(±0.5s 窗口无伤反弹×2 伤害可连发)、冲撞(60% 会心×2)。
- 敌人：坦克(炮弹,速度固定)/皮卡(机枪点射)/BOSS(2.2 倍体型红壳、攻速×2、专属 BGM)；7 关配额 20→30。
- 地形：荒地扬尘、雨天闪电(可劈敌)、河流/沼泽减速、岩石阻挡。
- 击破掉落：修理包/强化部件/强化装备×4（冲撞或反弹击破掉落率×3.2；BOSS 必掉大量）。

## 成长/存档
- 战地整备：每关后用部件升级 装甲/速度/攻击/防御（每级效果明确，可退回）。
- 存档 `localStorage.trSave`：关卡/分数/击破/用时/部件/升级/装备/周目；标题「继续存档」。
- 通关：撒花+滚动制作名单；Enter 进入下一周目（敌军 HP×(1+0.5×周目) 等缩放）；R 全部退回点数重新分配；无限周目。

## 音频
11 条内嵌 CC0 MP3（OpenGameArt），WebAudio 解码+首尾静音扫描无缝循环；状态机：标题/开场 jingle/关卡曲×3 轮换/BOSS intro+main/过关/失败/胜利/名单；合成器 SE 若干；M 静音。

## 渲染
480×270 像素世界离屏缓冲 → nearest-neighbor 整帧放大到显示画布（背板=显示尺寸×devicePixelRatio）→ 高清矢量 UI 层（720P+ 文字）；12 色色板；HUD 顶部/底部半透明底条；飘字走高清层。

## 已知 Minor（非回退项）
BOSS 血条标签对比度略低；触屏小屏 D-pad 与空袭标签间距近；帮助页个别中文断行。
