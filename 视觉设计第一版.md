可以。基于现在这版代码，我建议**不要直接在现有 `drawTank()/drawTerrain()/drawHUD()` 上不断堆特效**，而是做一次“保留玩法逻辑、重构视觉渲染层”的升级。

现版本来就采用了 **480×270 像素世界离屏 Canvas → nearest-neighbor 放大 → 高清 UI 层** 的双层结构，并关闭了图像平滑，这其实已经具备向“像素主体 + 高清光影/UI”的混合视觉升级的基础。 同时现有键鼠、手柄、触屏、难度、掉落、升级、多周目等玩法已经存在，不应为了视觉改造破坏它们。

## 一、推荐最终技术路线

不是直接照搬《八方旅人》的素材或具体画面，而是借鉴它最有效的**HD-2D视觉方法论**：

> **像素化主体 + 立体空间层次 + 景深 + 局部动态光源 + 高质量粒子 + 强烈明暗对比 + 现代高清UI。**

对于这个坦克游戏，我建议：

| 层级                | 技术                                     |
| ------------------- | ---------------------------------------- |
| 游戏规则/AI/碰撞    | 保留现有 JavaScript                      |
| 固定60Hz Simulation | JavaScript fixed timestep                |
| 主渲染器            | **PixiJS 8 / WebGL2**                    |
| 坦克/无人机         | PNG Sprite / SpriteSheet                 |
| 地形                | Tile + 大块背景纹理 + Decal              |
| 火焰/弹道/爆炸      | Particle Pool + additive blending        |
| 发光                | Emissive RenderTexture                   |
| Bloom               | Blur + additive composite                |
| 景深                | 前景/战场/远景分别 RenderTexture         |
| 环境光              | ColorMatrix + Overlay                    |
| UI                  | PixiJS高清层或DOM                        |
| 性能                | object pooling + atlas + batch rendering |
| 自动截图            | Playwright                               |
| 视觉校验            | 视觉模型对“参考图/实机截图”进行差异分析  |

### 为什么用 PixiJS，而不是继续纯 Canvas 2D

当前 Canvas 足够做玩法，但前面生成的效果图里有：

- 大量动态光源
- bloom
- 半透明烟雾
- 大面积火焰
- 景深
- 前后景分层
- emissive glow
- 数十辆单位同时战斗
- 大量弹道和粒子

继续用 Canvas2D 可以模拟，但代码复杂度和性能成本都会急剧上升。

**PixiJS 正好处在“2D游戏”和“GPU渲染”之间。**

没必要上 Three.js，也没必要把这个项目变成真正3D游戏。

------

# 二、不要重写玩法，先切成两层

目前类似：

```js
tick(dt);
draw();
```

改成：

```text
Game Simulation
      │
      │ GameState
      ▼
Render Snapshot
      │
      ▼
HD2D Renderer
 ├─ Background
 ├─ Terrain
 ├─ Props
 ├─ Units
 ├─ Projectiles
 ├─ VFX
 ├─ Lighting
 ├─ Atmosphere
 └─ UI
```

核心原则：

```js
updatePlayer()
updateEnemies()
updateShots()
updatePickups()
updateWeather()
```

这些尽量不碰。

增加：

```js
const snapshot = buildRenderSnapshot(gameState);
renderer.render(snapshot, interpolation);
```

这样以后视觉模型说：

> 爆炸光线太弱。

Codex只改 Renderer。

而不是去动 `hurtEnemy()`、`damagePlayer()`、AI等代码。

------

# 三、第一项必须先改：真正的60 FPS运动系统

现在代码是：

```js
p.a = Math.atan2(dy, dx);
```

也就是说，输入方向基本直接决定车体朝向。

建议改为：

```js
player.moveAngle
player.bodyAngle
player.turretAngle

player.velocityX
player.velocityY

player.targetVelocityX
player.targetVelocityY
```

然后：

```js
velocity += (targetVelocity - velocity) * acceleration * dt
bodyAngle += shortestAngle(bodyAngle, moveAngle) * turnSpeed * dt
```

即：

```text
输入
 ↓
目标方向
 ↓
加速度
 ↓
速度向量
 ↓
车体逐渐转向
```

这样不会再有“啪一下转45°”。

### 手柄

左摇杆：

```js
moveX = axis0;
moveY = axis1;
```

真正360°。

### 手机

目前四个方向按钮建议改为：

```text
左下：虚拟摇杆
右下：攻击区
```

真正360°。

### 键盘

WASD仍然只有8个目标方向，但通过：

- acceleration
- deceleration
- angular damping
- inertia

视觉上仍会非常丝滑。

------

# 四、坦克单位应该从“程序画图”切换成资产驱动

当前 `drawTank()` 是使用矩形画坦克。

下一版：

```text
assets/
  units/
    player/
      ironclad/
        hull.webp
        turret.webp
        tracks.webp
        emissive.webp

    enemy/
      scout/
      shield/
      artillery/
      flame/
      recon/
      commander/

    wingman/
      attack/
      defense/
      balanced/
```

关键点是：

**履带、车体、炮塔分开。**

例如：

```text
TankContainer
 ├ Shadow
 ├ TrackLeft
 ├ TrackRight
 ├ Hull
 ├ Turret
 │   └ Barrel
 ├ DamageFX
 └ Emissive
```

于是：

```js
tank.hull.rotation = bodyAngle;
tank.turret.rotation = turretAngle;
```

以后能够实现：

> 坦克向东北移动，但是炮塔向西射击。

战斗感觉会立刻提升一个等级。

------

# 五、我方单位视觉语言

前面生成的概念图可以固定为：

| 单位     | 主识别色 | 视觉特征               |
| -------- | -------- | ---------------------- |
| 主角坦克 | 蓝白     | 蓝色能量核心、重型炮塔 |
| 进攻僚机 | 红       | 尖锐、喷射、武器外露   |
| 防御僚机 | 蓝       | 圆形、护盾发生器       |
| 均衡僚机 | 金       | 对称、稳定、辅助模块   |

但我建议：

**玩家主坦克只有一种核心机体。**

不要现在就做三辆完全不同主坦克。

先让：

```text
IRONCLAD-07
```

成为游戏视觉Logo。

以后装备改变：

```text
炮塔
侧甲
导弹架
能量核心
履带
天线
```

玩家会从画面直接看到升级。

------

# 六、敌人必须做到“0.3秒内可识别”

前面效果图里的敌人设计方向是正确的。

| 敌人         | 造型语言         | 战场行为       |
| ------------ | ---------------- | -------------- |
| Scout        | 小、低、窄       | 高速绕后       |
| Shield       | 方、厚、巨大正面 | 正面推进       |
| Artillery    | 长炮管           | 远程           |
| Flamethrower | 油罐、橙红喷口   | 近距离区域控制 |
| Recon        | 黑、细、低反射   | 隐身/干扰      |
| Commander    | 高、宽、灯多     | 指挥/Buff      |
| Boss         | 普通单位约2倍    | 多阶段         |

因此不要仅仅依赖血条区分敌人。

**轮廓就是兵种。**

------

# 七、战场不要再是一层 Tile

建议分成：

```text
L0 Sky / Fog
L1 Far background
L2 Tall structures
L3 Ground base
L4 Ground detail
L5 Decals
L6 Props
L7 Units
L8 Projectiles
L9 Effects
L10 Lighting
L11 Foreground
L12 UI
```

例如废墟城市：

```text
远景：
烟囱
建筑
烟雾

中景：
废墟
集装箱
管道
铁路

战斗面：
柏油路
碎石
车辙
弹坑

前景：
铁丝网
断墙
钢梁
烟尘
```

这才会出现HD-2D的空间感。

------

# 八、目前最值得实现的四张地图

直接按照已经生成的设计继续：

### Training Base

视觉：

```text
冷蓝
实验设施
全息标线
移动靶
模拟障碍物
```

这里故意非常干净。

用于：

```text
武器练习
反弹
冲撞
连击
僚机
战术训练
```

------

### Ruined Industrial City

主视觉地图。

```text
黑灰
铁锈
火焰
断壁
铁路
工厂
浓烟
```

也是最适合展示游戏的地图。

------

### Desert Refinery

```text
暖黄
高亮火焰
大型储罐
管道
沙尘
油污
```

火焰区域可以真实成为战术要素。

------

### Frozen Fortress

```text
冷蓝
积雪
反光
雾
探照灯
堡垒
冰面
```

画面应与沙漠地图形成极强反差。

------

# 九、实现“八方旅人感”的核心不是像素，而是光

整个Renderer至少要有以下 RenderTexture：

```js
worldRT
emissiveRT
foregroundRT
fogRT
uiRT
```

渲染：

```text
World
  ↓
worldRT

火焰
炮口
激光
道具
爆炸
  ↓
emissiveRT
  ↓
Blur
  ↓
Additive

Fog
  ↓

Foreground
  ↓

Color grading
  ↓

UI
```

最终：

```text
World
+ Bloom
+ Fog
+ Foreground
+ Vignette
+ UI
```

------

# 十、爆炸应该至少拆成7层

不要再只做一个圆圈。

一次坦克爆炸：

```text
1 white flash       40–70 ms
2 orange fireball   150 ms
3 shockwave         300 ms
4 sparks            500 ms
5 debris            600 ms
6 smoke             2–4 s
7 ground decal      30–120 s
```

对应：

```js
ExplosionSystem.spawn({
    x,
    y,
    power: 1.0,
    color: "orange",
    debris: true,
    shockwave: true,
    light: 1.3,
});
```

BOSS：

```js
power: 3
cameraShake: 1
slowMotion: 0.15
```

视觉表现会差很多。

------

# 十一、连续攻击 Hit 系统专门做一个视觉控制器

建议：

```js
ComboController = {
  hits: 0,
  timer: 0,
  tier: 0,
  visualScale: 1,
  pulse: 0
}
```

规则：

```text
5秒没有有效命中
→ combo reset
```

每次：

```js
registerHit();
combo.timer = 5;
```

然后：

```text
10 HITS   GREAT
20 HITS   SUPERB
30 HITS   DOMINATING
40 HITS   RAMPAGE
50 HITS   UNSTOPPABLE
```

每10Hits：

```text
数字尺寸 +
屏幕震动 +
粒子 +
音高 +
Bloom +
文字动画
```

例如：

```js
scale = 1 + Math.min(hits / 100, 0.8);
```

但**不能遮挡坦克**。

建议位置始终偏战场中央上方。

------

# 十二、UI不要继续全部写死坐标

前面效果图可以归纳成：

```text
Top Left
玩家状态

Top Center
Wave/Boss

Top Right
Score/Minimap

Bottom Left
武器

Bottom Center
Combo

Bottom Right
技能/僚机
```

定义：

```js
HUDLayout = {
   playerPanel,
   wavePanel,
   minimap,
   weapons,
   combo,
   abilities
}
```

根据：

```js
safeWidth
safeHeight
aspectRatio
touchMode
```

布局。

不要：

```js
txt(..., 108, 5)
txt(..., 120, 6)
```

继续无限增加。

------

# 十三、UI设计语言

建议正式固定为：

### Base

```text
#080C12
#101720
#18232E
```

### Player

```text
#45B9FF
#8FDBFF
```

### Enemy

```text
#FF563F
```

### Reward / Combo

```text
#F6B83F
```

### Heal

```text
#66D889
```

### Epic

```text
#B36CFF
```

整体：

```text
半透明黑色面板
1px机械描边
轻微磨损纹理
小面积蓝色/金色发光
禁止大面积霓虹
禁止过度圆角
```

即：

> **工业军事UI + 极少量未来科技。**

------

# 十四、视觉模型不要负责直接生成全部游戏资产

这里很重要。

视觉模型最适合：

```text
概念图
单位设定
气氛
UI布局
颜色
材质
建筑风格
战场构图
```

不适合直接承担：

```text
严格32方向动画
连续sprite sheet
精确碰撞尺寸
所有帧完全一致
```

因此工作流应为：

```text
视觉模型
    ↓
Concept
    ↓
Visual Specification
    ↓
Codex
    ↓
程序化/手工资产
    ↓
实机截图
    ↓
视觉模型审查
    ↓
Codex调整
```

不要：

```text
图片模型 → PNG → 直接塞游戏
```

否则很快就会出现风格漂移。

------

# 十五、专门建立视觉回归场景

这是整个开发方案里我认为最重要的一项。

增加：

```js
G.visualScene("combat");
G.visualScene("units");
G.visualScene("boss");
G.visualScene("training");
G.visualScene("desert");
G.visualScene("snow");
G.visualScene("hud");
```

以及：

```text
?scene=combat&seed=42
```

固定：

```text
地图
敌人位置
玩家位置
掉落
Hits
天气
时间
```

这样 Codex 每次改完：

```bash
npm run visual:test
```

Playwright自动截图：

```text
artifacts/visual/combat.png
artifacts/visual/units.png
artifacts/visual/training.png
artifacts/visual/pause.png
```

再把：

```text
参考设计图
+
最新截图
```

交给视觉模型。

这是比“Codex自己觉得漂亮”可靠得多的办法。

------

# 十六、建议目录

```text
tank-battle/
├─ index.html
├─ package.json
│
├─ src/
│  ├─ game/
│  │  ├─ simulation.js
│  │  ├─ player.js
│  │  ├─ enemy.js
│  │  ├─ weapons.js
│  │  ├─ drops.js
│  │  └─ combo.js
│  │
│  ├─ render/
│  │  ├─ renderer.js
│  │  ├─ camera.js
│  │  ├─ terrain-renderer.js
│  │  ├─ unit-renderer.js
│  │  ├─ light-renderer.js
│  │  ├─ postprocess.js
│  │  └─ atmosphere.js
│  │
│  ├─ effects/
│  │  ├─ particles.js
│  │  ├─ explosion.js
│  │  ├─ smoke.js
│  │  ├─ projectile.js
│  │  ├─ shield.js
│  │  └─ combo-fx.js
│  │
│  ├─ ui/
│  │  ├─ hud.js
│  │  ├─ minimap.js
│  │  ├─ pause.js
│  │  ├─ mode-select.js
│  │  └─ loadout.js
│  │
│  └─ debug/
│     └─ visual-scenes.js
│
├─ assets/
│  ├─ units/
│  ├─ terrain/
│  ├─ props/
│  ├─ particles/
│  └─ ui/
│
├─ tests/
│  └─ visual.spec.js
│
└─ artifacts/
   └─ visual/
```

开发阶段可以模块化。

最终：

```text
npm run build
```

再打包成：

```text
index.html
game.js
assets/
```

仍然维持简单部署。

------

# 十七、推荐开发顺序

这里不要让 Codex 一次做完。

| Phase | 内容                    | 验证             |
| ----- | ----------------------- | ---------------- |
| P0    | 建基准截图/性能         | 原版功能无退化   |
| P1    | Simulation/Renderer分离 | 游戏玩法完全一致 |
| P2    | 60FPS + 360°移动        | 手柄/触屏流畅    |
| P3    | Pixi Renderer           | 战斗完全跑通     |
| P4    | 主角+6类敌军资产        | 单位可辨识       |
| P5    | 地图HD-2D化             | 4种地图区别明显  |
| P6    | Lighting/Bloom/DOF      | 达到参考图氛围   |
| P7    | 爆炸/炮火/烟雾          | 战斗观感成型     |
| P8    | HUD                     | 对齐效果图       |
| P9    | Hits系统                | 每10击反馈增强   |
| P10   | Wingman                 | 三种视觉/行为    |
| P11   | Training/Survival       | 模式完整         |
| P12   | 性能/移动端             | 60FPS稳定        |

------

# 十八、给 Codex 的主 `/goal`

下面这一份可以直接作为整个重构项目的主任务。

/goal

重构《钢铁咆哮·坦克大战》的视觉与渲染架构。

## 目标

在完整保留当前游戏核心玩法、AI、难度、输入、存档、升级、掉落、关卡和音频逻辑的前提下，把当前基于低分辨率 Canvas 程序绘制的视觉系统升级为高质量的 HD-2D 战术坦克游戏表现。

视觉方向参考已经提供的概念效果图，并借鉴“像素主体 + 立体空间层次 + 景深 + 动态光影 + 高清UI”的 HD-2D 方法，但不得复制任何第三方游戏的具体素材、角色、地图、UI或受版权保护的视觉资产。

最终效果应呈现：

- 像素化军事单位
- 具有明显空间层次的战场
- 强烈但克制的动态光影
- 火焰、烟雾、弹道和爆炸反馈
- 前景/战场/远景分层
- 局部Bloom
- 景深和空气透视
- 工业军事风高清HUD
- 蓝色玩家阵营、红色敌军、金色奖励的清晰视觉语言
- 60 FPS流畅战斗

## 最高优先原则

Gameplay Simulation 与 Renderer 必须分离。

不得为了视觉重构大规模修改已经验证可工作的：

- 敌人AI
- 武器伤害
- 掉落
- 难度
- 存档
- 多语言
- 音频
- Gamepad
- Keyboard
- Touch

先建立 RenderSnapshot，由Renderer读取游戏状态。

## 技术路线

优先采用：

- JavaScript
- PixiJS 8
- WebGL2
- RenderTexture
- Sprite Atlas
- Particle Pool
- Additive Blend
- Blur/Bloom
- ColorMatrix
- Playwright Visual Regression

不要把项目改成真正的3D游戏。

不要为了视觉表现引入不必要的大型框架。

## 运动系统

重构玩家移动，使：

player.moveAngle
player.bodyAngle
player.turretAngle

相互独立。

加入：

- acceleration
- deceleration
- angular damping
- steering smoothing
- velocity interpolation

Gamepad左摇杆必须支持真正360°移动。

触屏将现有四方向按钮升级为虚拟摇杆。

键盘WASD仍以8方向作为目标输入，但通过加速度和转向插值实现连续平滑运动。

游戏逻辑以固定60Hz Simulation运行，Renderer采用插值。

## 单位系统

把坦克视觉改成多组件：

TankContainer

- Shadow
- Tracks
- Hull
- Turret
- Barrel
- Equipment
- DamageFX
- Emissive

车体方向与炮塔方向必须可以独立。

玩家单位采用蓝白色能量视觉。

敌军建立至少以下视觉类型：

- Fast Scout
- Heavy Shield
- Artillery
- Flamethrower
- Stealth Recon
- Command Tank
- Boss

要求不依赖文字和血条，仅凭轮廓即可快速辨识兵种。

僚机建立：

- Attack / red
- Defense / blue
- Balanced / gold

三套视觉语言。

## 战场Renderer

至少分为：

L0 atmosphere
L1 far background
L2 tall structures
L3 terrain
L4 terrain details
L5 decals
L6 props
L7 units
L8 projectiles
L9 particles
L10 lighting
L11 foreground
L12 UI

实现至少四类视觉环境：

1. Training Base
   冷蓝、实验设施、全息标线、移动靶、模拟障碍。
2. Ruined Industrial City
   黑灰、铁锈、断壁、铁路、工业管道、火焰和浓烟。
3. Desert Refinery
   暖黄、储罐、炼化管道、火焰、沙尘、油污。
4. Frozen Fortress
   冷蓝、雪地、堡垒、冰面、雾和探照灯。

地图必须具有不同色调、光照、材质和环境粒子，而不是仅改变Tile颜色。

## 光影

建立：

worldRT
emissiveRT
foregroundRT
fogRT

火焰、炮口、激光、爆炸、能源、道具进入emissive pass。

emissiveRT经过Blur后使用Additive Blend叠加。

Bloom只能影响高亮对象，不允许整个画面普遍发光。

建立动态点光的视觉模拟：

- explosion light
- muzzle flash
- missile light
- burning object light
- pickup light
- shield light

需要控制同时活跃灯光数量。

## 景深

不要模糊核心战斗区域。

分别处理：

- far background
- gameplay plane
- foreground

远景轻微模糊；
游戏战斗层保持清晰；
距离镜头最近的部分前景可以有较强虚化。

## 爆炸系统

一次标准爆炸至少包括：

1. white flash
2. fireball
3. shockwave
4. sparks
5. debris
6. smoke
7. ground decal

Boss爆炸可以附加：

- screen shake
- stronger bloom
- debris burst
- short hit-stop or slow-motion

但不得影响操作稳定性。

## Combo系统

建立独立ComboController。

一次有效攻击刷新5秒计时。

5秒没有有效命中则combo归零。

每10Hits增加一次视觉和声音等级：

10 GREAT
20 SUPERB
30 DOMINATING
40 RAMPAGE
50+ UNSTOPPABLE

视觉增强包括：

- typography scale
- pulse
- particles
- bloom
- shake
- sound pitch/intensity

但Combo UI不得遮挡主要战斗目标。

## UI

重构为Anchor/Layout系统，禁止继续大量写死绝对坐标。

布局：

Top Left:
Player status

Top Center:
Wave / Boss

Top Right:
Score / Minimap

Bottom Left:
Weapons

Bottom Center:
Combo

Bottom Right:
Abilities / Wingman

视觉采用：

- dark translucent panel
- thin mechanical border
- subtle industrial texture
- cyan player highlight
- red enemy highlight
- gold reward/combo
- green heal
- purple epic

避免大面积霓虹、过度圆角和手游化塑料质感。

## Visual Test

建立固定视觉测试模式：

G.visualScene("combat")
G.visualScene("units")
G.visualScene("boss")
G.visualScene("training")
G.visualScene("city")
G.visualScene("desert")
G.visualScene("snow")
G.visualScene("hud")

支持：

?scene=combat&seed=42

所有视觉测试场景必须固定：

- random seed
- camera
- enemy position
- player position
- weather
- lighting
- pickups
- combo

使用Playwright自动生成截图到：

artifacts/visual/

至少包含：

combat.png
units.png
boss.png
training.png
city.png
desert.png
snow.png
pause.png

## 视觉模型辅助

每完成一个视觉阶段：

1. 自动运行游戏。
2. 生成固定视觉场景截图。
3. 将“概念参考图 + 当前实机截图”交给视觉模型。
4. 要求视觉模型只从以下方面分析差异：
   - composition
   - scale
   - silhouette
   - lighting
   - contrast
   - color
   - density
   - readability
   - UI hierarchy
   - VFX strength
5. 将分析结果转换成具体代码修改项。
6. 修改后重新截图。
7. 至少进行一次视觉复核。

视觉模型不得仅回答“更漂亮”“增加细节”等模糊意见，必须输出可以转化成CSS/Pixi/Shader/Particle参数的具体调整意见。

## 性能标准

目标设备：

Desktop:
稳定60 FPS。

Mainstream Android:
正常战斗场景尽量稳定60 FPS；
高负载允许动态降低：

- particles
- bloom resolution
- smoke amount
- shadow quality

不得改变游戏Simulation速度。

增加Performance HUD：

FPS
draw calls
sprites
particles
lights

## 开发方式

禁止一次性完成所有视觉重构。

分阶段执行：

P0 baseline
P1 renderer separation
P2 movement
P3 Pixi renderer
P4 units
P5 environments
P6 lighting
P7 VFX
P8 UI
P9 combo
P10 wingman
P11 training/survival
P12 optimization

每阶段必须：

执行
→ 测试
→ 截图
→ 视觉检查
→ 修正
→ 再验证

## 完成标准

可用才算结束：

1. 游戏可以正常启动。
2. 原有核心游戏功能没有明显退化。
3. 无console error。
4. 固定测试场景全部可以运行。
5. Playwright测试通过。
6. 关键视觉截图生成成功。
7. 60 FPS目标经过验证。
8. 输出本阶段改动摘要。
9. 输出验证命令。
10. 输出下一阶段入口。

不要只给计划。

从检查当前代码结构和建立P0视觉基准开始实际执行。

------

# 十九、每完成一版后给视觉模型的 `/goal`

这个我建议固定下来，之后反复用。

/goal

对当前《钢铁咆哮·坦克大战》的实机截图执行严格视觉审查。

输入中包括：

A. 已批准的游戏视觉概念图
B. 当前代码运行产生的实机截图

概念图只作为视觉目标，不要求逐像素复制。

请不要重新设计游戏，也不要提出与现有设计语言无关的新方向。

目标是判断当前代码实现与目标视觉之间最重要的差距，并把视觉意见转换成Codex可以直接实施的代码参数或资产修改要求。

依次分析：

1. Composition
   玩家、敌军、环境和UI的视觉重心是否正确。
2. Scale
   坦克、建筑、道具、爆炸、UI的比例是否合理。
3. Silhouette
   不同战斗单位是否能在0.3–0.5秒内通过轮廓区分。
4. Depth
   是否形成远景、战斗层、前景的空间层次。
5. Lighting
   主光、环境光、炮火、爆炸和能量光是否建立明显明暗关系。
6. Bloom
   发光是否只集中在高能量对象，而不是污染整个画面。
7. Color
   是否保持：
   玩家=蓝
   敌军=红
   奖励=金
   治疗=绿
   高级掉落=紫
8. Materials
   金属、混凝土、泥土、冰雪、火焰等材质是否容易区分。
9. VFX
   炮口、曳光、爆炸、火焰、烟雾、碎片和冲击波是否具有层次。
10. Readability
    特效是否妨碍玩家判断：
    敌军位置
    炮弹
    危险区
    掉落
    Boss攻击
11. UI Hierarchy
    是否能快速读取：
    HP
    Wave
    Boss
    Score
    Combo
    Weapon
    Wingman
12. HD-2D Feeling
    检查是否形成：
    像素主体
    立体环境
    局部动态光
    景深
    空气透视
    高对比灯光
    现代高清HUD

请输出：

A. 已达到的视觉目标

B. 最大的5个视觉差距
按影响从高到低排序。

C. 每个问题给出具体修改值，例如：

- sprite scale 1.0 → 1.18
- bloom radius 6 → 10
- background blur 2px → 4px
- smoke opacity 0.35 → 0.22
- UI panel alpha 0.55 → 0.72
- explosion spark count 12 → 28

如果不能确定精确值，给出合理区间。

D. 判断哪些问题属于：
CODE
ASSET
SHADER
PARTICLE
UI
COMPOSITION

E. 给Codex生成一份按优先级排列的修改任务。

限制：

不要用“更有质感”“更震撼”“更高级”等无法执行的语言作为最终建议。

所有建议必须尽可能转换成代码、shader、asset或layout调整。

最后给出：

Visual Match Score: XX/100

以及下一轮最值得优先修改的3项。

------

## 二十、视觉模型生成资产时，再使用第三种 `/goal`

它不是用来审查画面，而是专门用来控制**单位和地图设计一致性**。

/goal

根据已批准的《钢铁咆哮·坦克大战》视觉设计语言，为游戏资产制作设计参考。

保持以下统一视觉语言：

工业军事
近未来但不过度科幻
重型机械
像素化主体
真实金属结构
局部能源发光
高明暗对比
轻微磨损
战场使用痕迹

玩家阵营：
蓝 / 白 / steel
圆角机械结构与蓝色能源核心。

敌军阵营：
dark steel / red / rust
更加尖锐、侵略性强。

奖励：
gold。

治疗：
green。

高级能源：
purple。

所有单位必须保持：
统一视角
统一光线方向
统一尺寸比例
统一材质语言

不要生成完整游戏截图。

生成内容应服务于程序实现和Sprite制作。

针对每个单位提供：

正面设计
侧面设计
3/4方向设计
俯视设计
武器结构
关键颜色
发光区域
轮廓特征

当前单位：

Player:
IRONCLAD-07

Wingman:
Attack Drone
Defense Drone
Balanced Drone

Enemy:
Fast Scout Tank
Heavy Shield Tank
Artillery Tank
Flamethrower Tank
Stealth Recon
Command Tank
Boss

最重要标准：

即使全部变成黑色剪影，不同敌军仍然应该可以明显区分。

不要复制任何现有商业游戏的具体角色、载具或资产。

------

# 最后一个关键建议

这次不要把目标定成：

> **“让Codex把效果图做出来。”**

应该定成：

> **“让Codex建立一套能持续逼近效果图的视觉系统。”**

两者差别很大。

第一种通常做到第三轮就开始乱。

第二种有：

```text
视觉规范
↓
固定测试场景
↓
自动截图
↓
视觉模型审查
↓
参数化修正
↓
视觉回归
```

以后你继续提出“**BOSS再巨大一点、夜战更黑、爆炸再强、雪地图更冷、UI再简洁一点**”，Codex都能稳定迭代，而不是每次重新理解整个项目。

尤其你现在的程序已经不是纯Demo，现有 README 中列出的键盘、手柄、触屏、5档难度、掉落、强化、存档、多周目等已经形成了比较完整的玩法骨架。 **下一阶段真正应该重构的是表现层，而不是把已经完成的游戏逻辑推倒重来。**