# CHANGELOG — vNext 体验强化版

基线见 `BASELINE.md`；每阶段完成后须通过回归套件（主流程/触屏/性能）方可合入。

## v1.7 — 手感三改：震动分级 · 残影实体化 · 3D 等离子护罩 · 地形特效增强（2026-09-01）

- **震动分级（当前爆炸强度封顶，仅单位击破触发）**：`kickTier` 四档 `SHAKE_T`——T0 机枪命中 0.35 / T1 主炮命中·冲撞·连击升段 0.9 / T2 导弹·空袭·受伤·Breach 炮击 1.8 / **T3 单位击破 3.0（=原普通爆炸档，zoom 0.014）**；`explodeAt` 增 tier 参数（默认 big?3:1），命中路径传 T1/T2、`onEnemyDead` 统一 T3；玩家开炮后坐力降为微踢 0.5（移除每发 +1.2 随机震动）；BOSS 击破/玩家阵亡 = T3 + `shakeHold` 0.35s（震动延长不增强，hold 期间衰减暂停，衰减逻辑并入 `updCameraFX`）；全文件 ~16 处直接写 `ST.shake` 的散点清理，统一走 `kickTier`。
- **冲刺残影实体化**：ghost 快照系统 → `player.trail` 位置历史环（60Hz×1.5s）；冲刺中恒显 **3 条实心深色剪影**（深蓝黑三档 × alpha 0.92/0.78/0.62），**按距离回溯采样 40/80/120px**（1~3 车身长，不受河道减速压缩）；v15 帧图 `source-in` 生成实心剪影缓存（16 向×3 档懒生成），关平滑绘制、与本体同帧同尺寸 → 形象完全一致；像素回退 `drawGhosts` 用 `shade()` 压暗配色 + `drawTank` ghost 模式（无影无光）；松开 0.25s 淡出，传送 `tp` 清空历史。
- **护罩 3D 等离子球（纯代码实现）**：共享函数 `drawShieldOrb` 接管三条渲染路径（像素/aiart/v15）——接地影 + 球心底光 + **方向性明暗**（左上受光/右下阴影）+ 镜面高光 + 边缘增亮环 + 球面弧线（要塞盾三向弧纹/标准盾经纬线）+ 环绕能量点（带彗尾）+ 8Hz 闪现电丝 + 呼吸脉动 + 展开收缩入场 + 弹反白闪扩散环；半径 `shieldOrbR=11s+7s²` 随机体缩放（突击 16.5/均衡 18/重装 21.9，放大差异保肉眼可辨）；弹反各路径设 `shieldFlash`；HUD 冷却环硬编码 0.95 → 随机体 cd；帮助页护盾示意同步换新。
- **地形行进特效增强**：粒子上限 200/320/420 → 240/380/560；水面溅花 ×1.75 + **涟漪双圈错峰发射**（相邻触发交替大小圈，白/主题色，圆环几何可读）；冰面（L5）碎晶 ray 闪光 + 低平溅射；熔岩（L7）溅花 + 白金芯余烬 ×3 + 橙色动态光；减速区涡旋切向粒子 + 白/金亮色（解决工业关灰上灰不可见）+ 搅动光环 + 动态光；扬尘量/尺寸/寿命上调 + 雪沫白芯/沙地暖色/灰烬火星随主题分化；冲刺大烟团 ×2 + 速度线 ×2 + 碎石火花。
- **验证工具固化**：`tools/tankshot.js` 入仓（playwright-core 本地 Chromium）——7 静态场景 + 三机体护罩 + 弹反白闪帧 + 冰面冲刺残影（自动选取河道最平直段）+ 5 地形场景 + **震动分级数值断言**（记录 cameraKick 序列：0.35/0.5/0.9/0.45/1.8/3.0 各就位、命中 ≤T2、击破后 shake≥3）9 项全过，fps 71-80；`visualScene` 新场景 shield/shieldHit/free + `G.hull`/`G.findTile` 钩子；/tmp 冒烟脚本两处过时断言（选机体菜单链）修正后 17/17 全绿。
- **视觉验收闭环**：GLM-5.3-Flash 视觉 agent 五轮审校——护罩尺寸排序错误（v15 精灵宽度覆盖机体缩放，实测突击>均衡）、残影烟柱化（半透明压暗帧→实心剪影+距离采样）、减速区特效不可见（灰上灰）、水面涟漪涂抹（高频叠加→错峰发射）均修复至 PASS；像素回退路径（v15 禁用）同步验证零报错。

## v1.6 — 战斗表现升级：地形行进特效 + 冲刺残影 + BOSS 精英护卫队（2026-08-31）

- **地形行进特效 `terrainMoveFx`**（玩家/敌军共用，`effects.js`）：按所在 tile 与关卡主题出特效——水面/冰面/熔岩(河)向后上溅花(重力粒子+白沫, 密度4/拍)+扩散涟漪环，冲刺加白芯闪光；减速区(油污/能量/熔岩)亮色搅动粒子(主题色+50%白色火花, 工业关提亮至 #c2cbd4 保证暗油渍上可见)；普通地面主题扬尘(沙/雪/灰/土色)与冲刺拖尾烟团。履带 decal 保留。
- **真机一致性走查**（K40S, 注入式仪表测量）：7 关特效色彩随主题零串色；残影稳态 3-4 个跨地形一致；护卫队数量随进度一致（L1/L4=4、L7=5、二周目 L1=6）、精英 HP×1.25 实测命中、合围聚拢至目标半径、Boss 蓄力收缩、Boss 亡溃散；冲刺残影 fps 67 / BOSS 战 fps 46、零报错。视觉复审两轮：首轮"特效不可见"确认为截图时机问题（移动中重截后全部 PASS），次轮修复油污粒子对比度并闭环（车后亮像素 304 vs 背景路面 68）。
- **冲刺残影**：冲刺移动时每 0.07s 记录快照、存活 0.28s（稳态约 3-4 个），v15 HD 层随本体队列输出（渐隐 0.32 + 微缩，本体之下）；像素回退 `drawGhosts()` 半透明 drawTank。关卡切换清空。
- **BOSS 精英护卫队（不再单独登场）**：`spawnBoss` 随行 4+ 单位（4+min(周目,2)+后期+1，2/3 坦克+1/3 压制卡车，环布 BOSS 周身），精英化 HP×1.25/射速×0.85/速度×1.08/分×1.5；**围剿 AI**——各占玩家环形阵位（R≈136 缓慢旋转移位），BOSS 蓄力冲锋时收缩至 86 合围，BOSS 被击破后护卫溃散回常规 AI；出场 floater "精英护卫队随行"（三语言 `bossSquad`）；`visualScene('boss')` 场景同步补护卫。
- 验证：headless（残影稳态 3、护卫 4 个环布→1.5s 聚拢目标半径、Boss 死溃散、水花/扬尘截图）+ 真机（残影 3 个/沙尘拖尾 fps 67、BOSS 战 1+4 合围 fps 46）零报错。

## v1.5 — 关卡主题化视觉统一升级（2026-08-31）

**A. 关卡切换到新 7 主题体系**（与 v14 bright 开场图 / v15 地形图集两批资产同序对齐）
- L1 沙漠哨站 → L2 林地圣域 → L3 雨巷孤城 → L4 工业废墟 → L5 雪原突围 → L6 夜幕要塞 → L7 终焉战场；关卡名/提示三语言更新，**战斗数值不动**；沼泽机制由主题化减速区接替（L4 油污 / L6 能量地板 / L7 熔岩裂隙，id4 语义不变）。
- 调色集中化：`THEMES[]` 表统一提供每关整屏 tint、环境微粒、扬尘/水花/减速粒子色；`drawWorldGrade`/`initMotes`/玩家与敌军扬尘全部改读主题表。

**B. v15 地形图集接入（`src/game/v15terrain.js`）**
- `tools/assemble_v15_data.py` 扩展 terrain 段：每 stage 取 8 槽位（地面A/B、小径、水面、水岸、岩石、残骸×2）降采样 32px 拼条带 WebP 内联（7 条共 ~26KB）。
- 运行时启动一次烘焙为 16px 像素 tile（LANCZOS 源平滑降采样，hsh 确定性变体不闪烁）；`drawTerrain` 逐格 drawImage 替换程序化填色，tile id 玩法语义（减速/阻挡/弹丸消亡）零改动；水岸按上邻 tile 过渡；水面/减速区保留主题色微光动效；缺图逐级回退原像素绘制。
- aiart 旧 stage1 soft-light 地面叠图在 v15 生效时自动停用。

**C. v14 bright 开场图接入**
- `tools/convert_intros.py`：8 张 bright PNG（~21MB）→ WebP q85（~2.5MB）装入 `assets/stage-intros/`；`SPLASH_ART` 改按 THEMES 取图 + 遮罩整体减淡一档；Pages 部署清单、Android 资产目录同步换 webp（APK 26MB→9.7MB）；campaign manifest 状态 wired。

**D. 帮助页 + 全局面板玻璃化**
- `drawHelpScene` 13 页小场景全面换装：v15 精绘单位（玩家三机型/敌坦/卡车/僚机/空袭机）+ 苔藓地面 tile 俯视背景 + 落影；pg8 改为 7 主题地形 tile 展示；场景窗 160×84→200×96；像素绘制保留为缺图回退。
- `uPanel` 玻璃化：亮顶渐变 + 顶部镜面高光 + 双层描边 + 角标——标题菜单/整备/通关/失败/暂停/intro 卡全套受益；标题菜单透明度 0.55→0.66 保住玻璃层次。

**E. 虚拟按钮重设计**
- 全部虚拟按钮改圆形玻璃高透明（radial 渐变 + `backdrop-filter: blur(9px) saturate(1.35)`，不支持环境自动退化）；`#toast` 同步玻璃药丸样式。
- 四方向键合并为**模拟摇杆圆环**（`#joy`）：360° 连续方向 + 推杆幅度变速（死区 0.12），`VJ.ax/ay` 无手柄时合入 `PAD` 模拟轴（消费端零改动），超阈值同步四方向 VKEYS（菜单导航兼容），四向箭头点亮反馈；pointer capture 保证滑出环外仍可操控。
- **摇杆近透化**（真机反馈）：环内填充降至近全透（alpha ≤0.10、blur 8→3px），战场完全透出；环线加白（0.32）+内侧青色勾线+方向箭头提亮（0.52）并加投影，保证框线与方向清晰。
- **教学界面同步更新**：键位速览触屏布局（`drawCtrlTouch`）由方形方向键+方键图解改为摇杆圆环（`ctrlJoy`：四向箭头+摆动摇杆头+DRAG 标注）+ 圆形玻璃键（`ctrlGlassBtn`，与实机 `#joy`/`.tbtn` 同构同布局）；帮助第 1 页改为圆盘摇杆图示——推杆角与坦克车头方向实时联动演示；`h0d` 三语言文案补圆盘摇杆与新减速地形说明。

**验证**：CDP 触摸全链路（45° 推杆 VJ=(0.71,-0.71)、玩家朝向/速度/松手归零）；7 关地形+开场图+帮助页+摇杆截图经视觉审查（1 处 Major 为测试脚本拼接假象，已证伪；6 项 Minor 全部修复并复审）；单文件构建 17 张 sheet 全载、7 关可玩、零报错；冒烟 15 项通过（2 项已知过时断言）。

## v1.5（前序）— v15 俯视 16 方向单位资产接入（2026-08-31）

- **单位正交俯视化**：我方三机体 / 三种僚机 / 敌军坦克·运兵车 / BOSS 陆舰 / 空袭飞机 替换为 v15 正交俯视 16 方向精绘 sprite（`assets/ai-v15-topdown/`，逐帧实测角度表内嵌，`python3 tools/assemble_v15_data.py` 生成 `src/data/v15.data.js` 约 912KB WebP 内联）。
- **最近角度选帧**：生成的 sheet 帧序不统一（玩家坦克逆时针、僚机顺时针、步进不均、运兵车/BOSS 部分方向缺失）——运行时按每单位 angles 表取最近角度帧，缺失方向自动落到最接近可用帧，替代单一旋转/镜像方案。
- **接入层 `src/game/v15art.js`**：外层接管 `drawPlayer/drawEnemy/drawWingman/drawPlaneI` 与选车/选僚机预览，走 uctx 高清层输出（落影、阵营辉光、护盾环、血条、受击闪白与 aiart 管线一致）；v15 缺图时逐单位回退 v1.4 立绘/像素层。
- **僚机预览层级修复**：预览从 stage1 像素层移到 stage3 菜单之后绘制（`draw()`），不再被菜单列表遮盖（v1.4 起预览实际不可见的老问题一并解决）。
- **视觉验收三轮**：第一轮发现朝向整体反 180°（sheet 实际帧序与 manifest 标注相反）→ 改映射；第二轮自检抓到 N/W +45°（帧序顺逆混用）→ 逐帧实测角度表；第三轮复审通过。

## v1.3.3 — v14 高清 AI 立绘接入（2026-08-31）

- **单位全面立绘化**：我方三机体 / 三种僚机 / 敌军坦克·运兵车 / BOSS 陆舰 替换为手绘级高清裁切立绘（`assets/ai-v14/`，内嵌打包），HD 层平滑全向旋转、阵营辉光、受击闪白、血条/残影等细节随层输出；视觉验收两轮（4/10 → 9/10）。
- **渲染健壮性**：`AIART.flushHd` 对坐标异常的单位跳过绘制且保证队列必然清空（try/finally）——此前单个 NaN 单位会每帧抛 `createRadialGradient` TypeError、错误条常驻并可升级为崩溃。
- **文档同步**：导览战斗界面章节按新 HUD（单行信息条+武器芯片）与新触屏布局重写；说明书简介/机体预览措辞更新；HUD信息、战斗与触屏按钮、BOSS战、掉落与报酬、敌方单位、关卡图鉴 1-7 共 14 张截图以 v14 画面重拍。

## v1.3.2 — 可读性强化（2026-08-30 真机反馈）

- **帮助插图全面提亮**：`drawHelpScene` 背景由近黑改为天空+土层（战斗画面同级亮度），13 页插图全部清晰可辨。
- **操作说明补全**：帮助第 12/13 页（我方机体/僚机系统）新增专属插图——三机体实车预览（各自涂装/体型/名称标签）、僚机护航拦截示意（含僚机血条与拦截光环），不再复用战地整备的通用兜底图。
- **键位速览演示窗提亮**：演示窗区域回绘排除压暗遮罩（`drawCtrlIntro`），与实况游戏画面同等亮度。
- **标题画面可读性**：选单面板 0.82→0.55 半透明（透出封面坦克）；封面遮罩/暗角/顶压暗仅对 title 分支整体调亮一档。
- **文字断行**（BASELINE Minor 清账）：`wrapTxt` 增加行首标点悬挂与西文/数字整词换行规则。
- 说明书 `img/help/` 重拍为 13 页；导览相关截图（01/05/06/07/10/11）同步更新。
- 真机回归：完整一局（三武器+空袭/涡轮/护盾/拾取/僚机/BOSS/整备/通关/二周目/暂停子菜单/静音/语言往返）全程无报错。

## v1.3.1 — 页面导览实测修复（2026-08-30）

来自「页面与按钮导览」全交互实测（模拟器 + CDP 走查）发现的四个问题：

- **菜单残留命中区（P0）**：菜单关闭后 `MENU_RECTS` 不清理，旧按钮区在新页面点按无效且抛 `TypeError`（画面左下角出红色 ERR 条）；失败画面「点按屏幕重试」提示区正中招。修复：`draw()` 每帧无菜单时清空 `MENU_RECTS`（与既有 `TAP_RECTS` 同策略）。实测：over 界面中央点按直接重试、errStr 全程干净。
- **键位速览页文字叠压（P1）**：右侧演示窗下方标题/按键/说明三个 y 坐标互相踩（140/152/146 起）。修复：分区重排（136/149/160+i·9），三行文字互不重叠，4 行说明也收在底部动作条之内。
- **设置项触屏只进不退（P1）**：「难度」「BGM 音量」「音效 音量」用 clamp 到顶即卡死，触屏用户只能调高。修复：改为循环（与语言/虚拟按钮一致），4→0 回绕。实测：难度 4→0、BGM 4→0、音效 3→4。
- 说明书 4 张受影响截图已用修复后构建重拍（10/11/13/21/22）。

## v3.4 — HD-2D 视觉第一轮（视觉设计第一版.md 落地）

### 视觉回归基建（§15）
- `G.visualScene(name)`：combat/boss/units/explosion/weather/hud/title 七个固定场景（冻结刷兵、确定性摆位、可控特效），支持 `?scene=combat` 直达。
- 截图脚本 `/tmp/tankshot.js <tag>`（playwright-core+Chromium 本地栈）→ `output/visual/<tag>/*.png`；冒烟回归 `/tmp/tanksmoke.js` 17 项。

### 渲染改进（按视觉模型三轮审查落地）
- **敌军锈红重涂**（审查 Blocker）：锈红三阶板（#A63A28/#C9503A/#5E1F16）+ 红色阵营光；BOSS 红色核心灯脉动、尾部排气光点、炮管深灰+橙炮口；板条箱去红压暗 #3A3F45。
- **七层爆炸**（§10）：白闪→火球→冲击波→火花→碎片(重力)→烟雾(α0.4 久留)→弹坑 decal，附地面暖光池。
- **Decal 层**（§7 L5）：履带压痕（玩家+敌军，冲撞加宽加深）与弹坑焦痕关卡内持久。
- **掉落信标光柱**：40px 垂直光柱+顶端光点+呼吸地圈；火控芯片改紫色（高级掉落=紫，与锈红敌军区分）。
- **氛围包**：暗角 0.68、顶部远景压暗、3 处掩体暖光池（呼吸）、环境漂浮微粒（干旱沙尘/荒原余烬/绿野萤光/沼泽湿气）。
- **环境色调分级**（§8）：干旱暖沙/荒原铁锈/雨图冷蓝/沼泽阴绿；雨图绿收敛、地面勾边去青蓝。
- **HUD**：面板 α0.78–0.85、BOSS 血条 25% 分段刻度+白色 ghost 滞后条（~12%/s）、连击段位词 GREAT→UNSTOPPABLE（面板上方）、曳光增强（敌弹橙色）。
- **战斗反馈**：炮口火光（玩家机枪/主炮/导弹+敌坦克/皮卡）、玩家蓝色能量核心脉动光（§五）。
- **标题夜景重建**：两层城市剪影+窗光+旋转探照灯扇形，删除金虚线。

### 验收
- 视觉模型三轮审查：基线 58/100 → v2 76/100（10 任务 9 PASS+1 PARTIAL）→ v3 聚焦复核 6/6 PASS，0 Blocker/0 Major。
- 冒烟回归 17 项全过（intro/play/移动开火/god/过关/整备/下一关/7 场景/死亡重试），零 console error，~65–72 FPS，renderMs≈0.7。

## v3.3 — Combat Foundation（第三版计划·Phase 1，本次）

### A｜DamageEvent 统一伤害归因
- 所有伤害走 `applyDamage(e,dmg,cause,opts)`：构造 DamageEvent（cause/parentId/chainDepth/comboEligible），`chainDepth≤3` 防无限连锁。
- cause 全表：machinegun/cannon/missile/**reflect/perfectParry**/**ram/breach/knockback/collision**/explosion/**chainExplosion**/wingman/lightning；STATS 按 cause 记录伤害量与次数（G.stats() 可查）。
- 击杀归因：冲撞/破门/击飞撞/连锁爆炸致死的敌人会殉爆并按 chainDepth 继续传导。

### B｜Parry 双层分级
- **普通格挡**：小型白蓝接触环 + 30–40ms 顿帧 + 金属轻音，无大字。
- **Perfect Parry**（举盾 ≤0.12s 窗口）：55ms 顿帧、四向十字光束、0.28s 小号 "PARRY" 文字、高音双 beep、反弹弹丸伤害 ×2.5/速度 ×2.1（普通 ×2/×1.7）、**连击宽限保底 1.2s**。

### C｜Breach Ram 状态机 + 质量分级
- 高速（>120）正面撞上敌人 → **BREACH_LOCK 0.34s**（顶住目标、火花、可随时主炮）→ **BREACH_FIRE 零距离炮击 ×1.35**（大冲击波+白芯+10 根放射光线、55ms 顿帧）→ 敌人按质量 **light/medium/heavy/fortress** 击飞（400/300/160/0 px/s）→ 撞上其他敌人产生 **collision/knockback** 伤害与殉爆连锁；BOSS（fortress）不击飞、改为 1.5s 大硬直。
- 新连击权重：机枪+1/主炮+2/导弹+2/反弹命中+2/Perfect反弹+2(命中时)/破门撞+2/零距离炮击+3/击飞撞+2(每目标)/连锁爆炸+1(每目标)/击破+1/精英+2/BOSS+5。

### 视觉与验收修复（本轮视觉验收发现）
- 修复 flash 粒子落进兜底分支导致的大半透明色块糊住画面；冲击波环改为全程可见的扩散环（55%→100% 半径）。
- 零距离炮击特效升级：白芯 ≈1.9 车长、扩散冲击波 + 10 根放射光线（寿命 0.2s+），顿帧期间也清晰可读。
- Perfect Parry 十字改为光束拖尾；PARRY 文字提到 10px。
- 连击面板宽度上限 148px，高段位不再压到两侧技能面板。
- `G.tp` 调试钩子同步吸附相机（消除相机拖影导致的截图偏差）；`breachFire` 防御性清除残留锁定。

### 验证
- 回归全过：主流程 39 / 触屏 12 / vNext 专项 13 / 战斗链专项 verify9 23（归因、Parry 分级、Breach 全链、连锁深度上限、顿帧）×多轮稳定；性能专项 60.0Hz。
- 视觉验收：5 帧战斗链截图（parry/lock/fire/flying/aftermath）经视觉模型像素级验收通过（PARRY 文字+十字+反弹弹丸、冲击波+放射光线、击飞与敌敌碰撞、HUD 无遮挡无重叠）。

## v3.2 — PHASE 1 移动 + PHASE 2 连击

### PHASE 1｜360° 全向移动
- 移动从八方向离散改为 **速度向量模型**：`inputVector → targetVelocity`，加速度推进（~0.16s 到极速）、摩擦减速（~0.2s 停止），保持"有份量但灵活"的坦克手感。
- 车体朝向以 10 rad/s 平滑跟随速度方向；WASD 向量叠加 + 手柄**模拟摇杆原生 360°输入**（引擎新增 PAD.ax/ay 模拟轴，死区 0.18）。
- 僚机/炮塔独立瞄准留待后续阶段（按文档：炮塔先维持现有逻辑）。
- 像素车体旋转角量化从 15° 细化到 7.5°，转向观感更顺。
- 敌方预判瞄准自动受益于真实速度向量。

### PHASE 2｜连击系统
- **5 秒连击链**：机枪命中 +1 / 主炮·导弹命中 +2 / 击破 +3 / 冲撞击破 +4 / BOSS +5 / 反弹 +3，5 秒未命中归零。
- **Hits xN HUD**（三语）：右侧分数下方，颜色白→沙→金→红随段位升级，剩余时间条。
- **每 10 连阶段强化**：10 闪光+音效 / 20 描边发光+轻震 / 30+ 击破冲击环增强 / 40+ 尾焰 / 50 大字播报 / **60+ Overdrive 状态**（射速 ×1.15、移速 ×1.1、金色尾焰）。
- **掉落倍率**：10/20/30/40 连 → +10%/+20%/+35%/+50%（40+ 额外提高稀有权重）。
- 爆炸音效随连击段位分层增亮。

### 验证
- 回归：主流程 39 项 / 触屏 12 项 / 性能专项全过；新增移动模型与连击规则专项测试（verify8）。
- 视觉：连击 HUD、Overdrive 状态截图经视觉模型按第十五章标准验收。

## v3.1 — 架构重构（PHASE 0~2 of 战役化计划）
- src/ 模块化（12 文件）+ index.dev.html + build.js 构建单文件。
- 60Hz 固定逻辑 + rAF 插值渲染 + 对象池 + 空间网格 + F3 性能面板 + AUTO 画质档。

## v2.x — 功能基线
见 BASELINE.md（三端操作/三语/难度/掉落强化/存档/NG+/内嵌音乐/高清 UI 层）。
