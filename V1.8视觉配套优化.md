整体方向是对的，但这份需求书目前更像“**AI美术资产重制规格**”，还没有完全升级成“**UI系统重设计规格**”。尤其 v1.8 已经加入双摇杆、独立炮塔、多锁导弹、技能冷却、漂移和更复杂的护盾反馈，UI 的首要任务应该从“换漂亮边框和图标”变成：

> **让玩家在高速战斗中 0.5–1 秒内知道：我还能不能打、正在锁谁、锁了几枚、技能什么时候好、炮塔正指哪里、当前是否处于危险/强化状态。**

当前文档对 AI 美术生产和单位资产管线已经相当成熟，但 UI 信息架构、状态系统和移动端适配仍有几个需要强化的地方。

------

# 一、先改一个最重要的问题：阵营色规则目前存在内部冲突

目前定义：

- 突击：锈橙 + 金
- 重装：青绿 + 红热核心
- 敌军：暗砖红 + 警红
- 爆炸：金 / 橙 / 白
- 锁定：金 / 青

这里会发生两个问题。

### 1. 突击机容易和敌军/爆炸混色

高速战斗时：

```text
突击机橙金
+
敌军砖红
+
爆炸橙金
+
锁定框金
```

会形成暖色拥挤。

### 2. 重装的红热灯容易被识别成敌军 IFF

所以建议增加一个**全我方统一 IFF 色**：

```text
FRIENDLY IFF = 冷青蓝 #35C8FF / 冷白
ENEMY IFF    = 警红 #FF4C3F
```

三个玩家机体不管主装甲是什么颜色，都必须至少包含：

- 炮塔小型冷青 IFF 灯；
- 车尾冷青识别灯；
- 必要时受击轮廓短暂冷青描边。

于是：

```text
突击：
橙色 = 职业/机体个性
青蓝 = 我方身份

均衡：
钢蓝 = 职业
青蓝 = 我方身份

重装：
暗铁 = 职业
青蓝 = 我方身份
红热 = 内部机械热源，不承担识别
```

这样比现在单纯“敌暖我冷”更可靠。

建议把 §2.2 改成：

> **阵营身份色优先于机体职业色。**

这是 P0。

------

# 二、UI 最好重新分成三个层级，而不是统一“美术化”

我建议建立明确的 **Decoration Budget（装饰预算）**。

## Level 1：战斗 HUD —— 极简

这里不要大量金属边框。

应该主要是：

```text
细线
半透明暗底
状态色
图标
进度环
短动画
```

战斗中最贵的是屏幕空间，不是素材数量。

## Level 2：暂停 / 帮助 / 设置 —— 中等装饰

可以使用：

- 9-slice；
- 金属角；
- 铆钉；
- 青色光边。

但正文阅读区域依然必须干净。

## Level 3：标题 / 整备 / 结算 —— 高装饰

这里才适合：

- AI 插画；
- 复杂机械框；
- 徽章；
- 大面积背景；
- 角色视觉。

也就是说：

> **不要让战斗 HUD 和标题菜单具有相同美术密度。**

否则会明显增加视觉噪声。

------

# 三、建议重新设计整个战斗 HUD 信息架构

目前 P1 主要写了“武器图标”和“框”，但还没有定义各信息放在哪里。

建议固定一个 16:9 HUD Contract。

## 推荐布局

```text
┌───────────────────────────────────────────────┐
│ HP / ARMOR             BOSS             SCORE │
│ SHIELD                                  HITS │
│                                               │
│                                               │
│               战斗世界                         │
│        锁定框 / 炮塔瞄准线 / 飘字              │
│                                               │
│                                               │
│ Turbo                     Weapon / Skill      │
└───────────────────────────────────────────────┘
```

但针对不同设备不要完全一样。

### 键盘 / 手柄

底部可以显示：

```text
MG  Cannon  MSL  Air  Turbo  Shield
```

形成紧凑武器条。

### 触屏

**不要再放一份武器 HUD。**

因为右侧已经有：

```text
机枪
主炮
导弹
空袭
护盾
右摇杆
```

如果 HUD 又出现六个武器图标，就是信息重复。

触屏模式应该让：

> **按钮本身就是 HUD。**

例如导弹按钮直接显示：

```text
图标
+ 外圈蓄力
+ 冷却遮罩
+ 当前锁定数
```

护盾按钮：

```text
Ready   → 青白亮
Perfect → 脉冲
CD      → 灰化+环形倒计时
```

这会明显降低手机画面拥挤程度。

------

# 四、多锁导弹 UI 建议改一个关键设计

现在计划：

> 每目标叠多准星。

我建议不要真正叠 2～6 个完整准星。

在高速战斗中会变成：

```text
((((TARGET))))
```

特别是同时锁 3 个单位时非常杂乱。

## 推荐方案

**一个目标只画一个主锁定框。**

锁定数量使用外围小节点：

```text
       ●
   ●       ●
      [TARGET]
   ●       ●
       ●
```

例如：

- 1枚：1个亮点；
- 3枚：3个点；
- 6枚：完整六节点。

或者：

```text
[ TARGET ]  ×3
```

数字仍由代码绘制。

### 锁定状态建议三阶段

```text
Searching
↓
Acquiring
↓
Locked
```

颜色：

```text
Searching  深青灰
Acquiring  青
Locked     金白
Lost       快速红闪 → 消失
```

这样玩家真正看得懂“导弹正在干什么”。

------

# 五、CD/蓄力最好形成一个统一的 UI 状态语言

v1.8 已经有大量状态：

```text
Ready
Pressed
Charging
Cooling Down
Disabled
Perfect Window
Overdrive
```

建议所有技能图标都遵守同一套规则。

例如：

| 状态      | 表现              |
| --------- | ----------------- |
| Ready     | 原色+100%亮度     |
| Pressed   | 105%放大+短白闪   |
| Charging  | 顺时针环形填充    |
| Full      | 金白脉冲          |
| Cooldown  | 去饱和+逆时针恢复 |
| Disabled  | 35%亮度           |
| Perfect   | 青白快速脉冲      |
| Overdrive | 外圈金色动态边    |

这意味着：

> 不需要给每个状态再生成一张图片。

只需要：

```text
Base icon
+
Canvas tint
+
mask
+
arc
+
scale animation
+
glow
```

这才是最划算的 UI 美术升级。

------

# 六、P1 的资产数量实际上可以明显减少

目前规划：

- 金框
- 青框
- 红框
- HUD 图标
- Touch 图标
- 三个头像
- 等

我认为里面存在重复资产。

## 1. 三种框 → 一种框

AI 只生成：

```text
UI-FRM-BASE
```

最好是：

> 中性银黑金属 + grayscale highlight mask。

然后程序：

```text
Gold tint
Cyan tint
Red tint
```

实现三种状态。

优势：

- 风格100%统一；
- 少两张纹理；
- 色彩可以动态改变；
- Boss warning甚至可以动画红闪。

------

## 2. HUD 图标与触屏图标合并

现在：

```text
UI-ICO-WPN
UI-ICO-ACT
```

实际上前 6 个完全重复。

改成：

```text
UI-ICO-MASTER
```

7个：

```text
MG
Cannon
Missile
Airstrike
Turbo
Shield
Pause
```

HUD和Touch直接复用。

------

## 3. 三张机体头像可以取消

这是我非常建议的一项。

现在已经拥有高质量：

```text
Hull
+
Turret
```

完全可以实时绘制一个：

```text
徽章框
+
实际游戏坦克
```

例如：

```text
╔════════╗
║   🛡   ║
║ Tank   ║
╚════════╝
```

机体部分直接调用游戏素材。

这样：

- 永远不会出现“头像和实际坦克长得不一样”；
- 将来换机体不用重画头像；
- 可以动态展示炮塔；
- 减少3次AI生成。

AI只需要生成一个：

```text
UI-BADGE-FRAME
```

即可。

------

# 七、标题图的规格建议直接修改

当前：

> UI-TITLE 1024×1024。

不建议。

游戏本身是横屏 480×270：

```text
16:9
```

方形母版会导致最终大量裁切，而且构图不可控。

## 建议

生成：

```text
1536×864
或
1920×1080
```

如果生成模型只能稳定输出特定比例，就至少使用：

```text
1536×1024
```

并定义：

### Safe Composition Zone

```text
中央 70% = 永远不能裁掉的重要主体
左 35% = 标题/菜单留白区域
右/下 = 玩家三机体
远景 = 敌军
```

比如：

```text
┌─────────────────────────────────────┐
│          敌军 / 烟尘                 │
│                                     │
│   TITLE          均衡                │
│   START     突击       重装           │
│                                     │
└─────────────────────────────────────┘
```

最重要的是：

> AI插画必须明确给 UI 留 **negative space**。

否则再漂亮也会和菜单打架。

------

# 八、9-slice 方案建议技术上再简化

当前要求：

> 1024×1024完整空心框。

这个源素材可以保留，但**最终运行时纹理没必要是1024²**。

9-slice真正需要的是：

```text
corner
edge
center
```

边缘细节。

建议：

```text
AI Source: 1024×1024
↓
处理
↓
Runtime: 256×256 或 512×512
```

中心直接 alpha=0。

而不是：

```text
中心 #0a0f14
```

这样：

- 面板颜色继续由 Canvas 控制；
- 同一框可用于不同透明度；
- 少 overdraw；
- 更方便 tint。

------

# 九、目前“解码纹理内存 ≤12MB”的预算需要重新算

这里是需求书里一个比较明显的技术问题。

按目前计划的单位 atlas：

```text
6 × 512² 玩家
+ 3 × 512² 敌/支援
+ 1 × 768² BOSS
+ 3 × 384² 僚机
```

仅单位 RGBA 解码纹理大约已经是：

**≈13MB。**

如果再加：

```text
2 × 1024² UI atlas
```

又约：

**8MB。**

也就是说如果同时驻留：

> **已经约21MB，而不是9MB。**

标题图、车库背景若也是1024²解码后还会继续增加。

所以 §6 的：

```text
解码纹理内存 ~9MB
硬顶 ≤12MB
```

目前无法和素材规格同时成立。

## 推荐解决方式

不是简单压 WebP。

WebP 只能减少：

```text
文件体积
```

不会显著减少：

```text
RGBA解码后的纹理内存
```

应该做：

### Lazy Decode / Load

战斗只加载：

```text
当前玩家 Hull
当前玩家 Turret
当前关卡敌人
当前僚机
当前Boss（需要时）
HUD
```

标题图退出 title 后释放。

整备背景只在 upgrade 页加载。

### UI最终纹理降级

例如：

```text
Frame：256/512
Icon atlas：256/512
Badge：程序绘制
```

我建议把这项升级为 **P0 Asset Budget 修订**。

------

# 十、AI 图标需要增加一个“24px测试”

目前虽然写：

> readable at 24px

但应该正式加入验收。

生成后每枚 icon 都自动输出：

```text
128 px
64 px
32 px
24 px
```

Visual QA 最后只看：

```text
24px
```

问一个问题：

> 不看文字，0.5秒能不能认出是什么？

尤其：

```text
主炮
导弹
Turbo
Airstrike
```

很容易缩小以后都变成“三角形”。

因此图标风格应更接近：

```text
80% silhouette
20% inner detail
```

而不是精细插画。

------

# 十一、触屏UI建议再做一层专门设计

这是此次 UI 重做里很重要的一项。

因为 v1.8 已经变成：

```text
左摇杆
+
右摇杆
+
武器按钮
```

建议 Touch UI 也定义三个状态。

### Idle

```text
opacity ≈ 0.35
```

### Touching

```text
opacity ≈ 0.75
scale 1.05
```

### Combat-critical

例如：

```text
导弹锁满
护盾Ready
Turbo恢复
```

局部提高到：

```text
opacity ≈ 0.9
```

而不是所有按钮一直亮着。

这会让战斗画面干净很多。

------

# 十二、建议增加 UI Design Tokens

这是当前需求书最缺的一层。

否则：

> AI画框是一种颜色，Canvas HUD又是另一种颜色，触屏CSS又出现第三套颜色。

建议建立：

```js
UI_THEME = {
  bg: '#0a0f14',

  friendly: '#35c8ff',
  enemy: '#ff4c3f',

  gold: '#f6b94e',
  white: '#eef6ff',

  panelAlpha: 0.72,
  hudAlpha: 0.58,

  borderThin: 1,
  borderNormal: 2,

  glowSmall: 4,
  glowLarge: 10,

  cornerSmall: 4,
  cornerPanel: 8
}
```

再加 typography：

```text
HUD Small
HUD Normal
HUD Important
Title
Warning
```

和 spacing：

```text
4 / 8 / 12 / 16 / 24
```

这样才能真正叫“UI系统”。

------

# 十三、整备页应该重点重新设计，而不只是换背景

现在规划主要是：

> 车库背景 + 五行升级。

我建议把它做成：

```text
┌────────────────────────────────────────────┐
│ FIELD REFIT                         PARTS 8 │
├──────────────┬─────────────────────────────┤
│              │ Armor        LV 12   +180   │
│   当前坦克    │ Speed        LV 08    +40%  │
│              │ Attack       LV 10    +80%  │
│ 可缓慢旋转    │ Defense      LV 05    30%   │
│              │ Cooldown     LV 06   -19%   │
├──────────────┴─────────────────────────────┤
│ 当前最终属性 / 部件 / 出击                   │
└────────────────────────────────────────────┘
```

这里使用真实分层坦克实时旋转。

它会比三个 AI 头像有价值很多。

同时能够直接展示：

```text
body
turret
不同装甲轮廓
```

成为这次美术重制的展示窗口。

------

# 十四、可以增加少量 UI 动效，但不要增加更多贴图

如果希望“日系游戏质感”继续提升，收益最高的未必是更多PNG，而是：

### Select

```text
光条扫过
+ 边框短亮
```

### Confirm

```text
frame 收缩2%
→ 回弹
```

### Missile Full Lock

```text
chip pulse
+ reticle snap
```

### Shield Ready

```text
一次非常轻的环形波
```

### CRUSH / Perfect

```text
短 hitstop
+ UI flash
```

基本都可以代码实现。

这会比再增加十几张美术图更接近商业游戏。

------

# 十五、我建议把 P1 重构为下面的资产表

原来约12张生成素材，可以压缩成：

| ID            | 素材            | 建议                   |
| ------------- | --------------- | ---------------------- |
| UI-TITLE      | 横屏标题插画    | 保留                   |
| UI-FRM-BASE   | 中性9-slice母框 | **1张代替金/青/红3张** |
| UI-ICO-MASTER | 7枚统一图标     | **HUD/触屏共用**       |
| UI-BADGE      | 通用机体徽章框  | 实际坦克实时绘制       |
| UI-BG-DEP     | 整备背景        | 保留                   |

也就是核心 UI AI 素材可以从约：

**12张 → 5张左右。**

剩下的视觉提升来自：

```text
Tint
Glow
Animation
Mask
Progress arc
Dynamic tank render
```

反而更统一。

------

# 十六、建议增加一个独立的「UI验收章节」

目前 §7 更偏资产验收。

建议新增：

## UI Functional Visual QA

### HUD

- 0.5秒能找到 HP；
- 0.5秒能判断导弹 Ready/Charging/CD；
- 多锁目标和锁定数量可理解；
- Shield Ready/CD明显区分；
- Hits 60+ 不遮挡战斗；
- Boss + 6 missiles + explosion 同屏仍能识别玩家。

### Touch

- 双摇杆不会被 UI 遮挡；
- 不看文字也能识别六技能；
- 24px icon PASS；
- 手指覆盖按钮时仍能判断 CD；
- 刘海/圆角/safe-area PASS。

### Menu

- 中文/日文/英文均无溢出；
- 所有面板可拉伸；
- 低亮/高亮背景文字对比正常。

### Performance

- UI动画不新增对象；
- 图标/框统一纹理复用；
- 页面切换释放非当前场景大图；
- 解码纹理实际峰值记录。

------

# 最终建议：把这次重制拆成“单位美术”和“UI系统”两条线

当前文档的 **P0单位重制部分基本已经成熟，不需要大改**。

真正值得重新设计的是 P1，我建议从：

> “给现在 UI 换图标、换框、换背景”

升级成：

> **“建立一套 Tactical HD-2D UI Design System。”**

核心原则可以收敛成六条：

1. **阵营身份色 > 机体职业色**；
2. **战斗UI极简，菜单UI华丽**；
3. **同一图标承担所有输入模式，不重复资产**；
4. **状态主要靠代码动画、进度和 tint，而不是多张图片**；
5. **触屏按钮本身就是技能HUD，避免重复显示**；
6. **AI负责高价值视觉母版，程序负责一致性、响应式和状态变化。**

如果这样调整，我认为这一轮视觉升级会比简单“AI全面换皮”稳定很多，也更容易形成真正统一的游戏视觉语言。